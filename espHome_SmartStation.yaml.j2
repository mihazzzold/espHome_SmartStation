{% set device_name = serial_number | default("espHome-SmartStation") %}
{% set friendly_name = ("espHome SmartStation #" ~ serial_number) | default("espHome SmartStation") %}
{% set firmware_version = "0.1.1-beta" %}

esphome:
  name: "{{ device_name }}"
  friendly_name: "{{ friendly_name }}"
  project:
    name: "mihazzzold.espHome-SmartStation"
    version: "${firmware_version}"

# dashboard_import закомментирован, так как ESPHome 2026.2.4 может иметь проблемы с валидацией J2 URL 🛠️
# dashboard_import:
#   package_import_url: "https://raw.githubusercontent.com/{{ github_user | default('mihazzzold') }}/{{ github_repo | default('esphomeFlasher') }}/main/firmwares/espHome_SmartStation/espHome_SmartStation.yaml.j2"
#   import_full_config: false

esp32:
  board: {{ board | default("esp32-c3-devkitm-1") }}
  variant: esp32c3
  framework:
    type: esp-idf

wifi:
  ap:
    ssid: "{{ device_name }}-setup"
    password: !secret ap_password
  reboot_timeout: 0s

captive_portal:

logger:
  level: WARN
  hardware_uart: USB_SERIAL_JTAG

api:
  encryption:
    key: !secret api_encryption_key

ota:
  - platform: esphome
    password: !secret ota_password
  - platform: http_request

# Настройка I2S аудио (Общий порт для входа и выхода на ESP32-C3)
i2s_audio:
  - id: i2s_bus
    i2s_lrclk_pin: {{ speaker_lrclk | default("GPIO3") }}
    i2s_bclk_pin: {{ speaker_bclk | default("GPIO2") }}

# Динамик (ЦАП PCM5102A)
speaker:
  - platform: i2s_audio
    id: speaker_id
    i2s_audio_id: i2s_bus
    i2s_dout_pin: {{ speaker_dout | default("GPIO1") }}
    dac_type: external
    # PCM5102A не требует MCLK (может генерировать его сам из BCLK)

# Микрофон (INMP441)
microphone:
  - platform: i2s_audio
    id: microphone_id
    i2s_audio_id: i2s_bus
    i2s_din_pin: {{ mic_din | default("GPIO6") }}
    adc_type: external
    pdm: false
    # INMP441 работает в обычном режиме I2S, используя общие шины BCK/LRCK с ЦАП

# Голосовой ассистент 🎙️
voice_assistant:
  id: va
  microphone: microphone_id
  speaker: speaker_id
  # Использование Home Assistant для распознавания (STT/TTS/Wake Word) 🤖
  # Необходима конфигурация в Home Assistant (Assist) 🏠
  noise_suppression_level: 2
  auto_gain: 31dBFS
  volume_multiplier: 2.0
  on_error:
    - logger.log:
        format: "Voice Assistant Error!"
        level: ERROR
  on_client_connected:
    - logger.log: "Voice Assistant client connected to Home Assistant"
  on_client_disconnected:
    - logger.log: "Voice Assistant client disconnected"
  on_tts_start:
    - logger.log: "Voice Assistant: Speaking..."
  on_stt_end:
    - logger.log:
        format: "Voice Assistant: Recognition finished: %s"
        args: [ "text" ]

  # Настройка HTTP клиента для обновлений
http_request:
  id: http_client
  timeout: 10s
  useragent: "ESPHome/{{ firmware_version }}"
  verify_ssl: true

# Нативная сущность обновления в Home Assistant (доступно с ESPHome 2023.9.0+)
update:
  - platform: http_request
    id: ha_update_entity
    name: "Firmware Update"
    entity_category: diagnostic
    # Для приватного репозитория используем raw URL
    source: "https://raw.githubusercontent.com/mihazzzold/espHome_SmartStation/main/latest.json"

globals:
  - id: available_version
    type: std::string
    restore_value: no
    initial_value: '"{{ firmware_version }}"'
  - id: update_url
    type: std::string
    restore_value: no
    initial_value: '""'

# Опционально: кнопка для активации (Push-to-talk)
binary_sensor:
  - platform: gpio
    pin:
      number: {{ action_button | default("GPIO9") }} # Кнопка BOOT на SuperMini (иногда требует внешнюю подтяжку)
      inverted: true
      mode: INPUT_PULLUP
    name: "Кнопка активации"
    on_click:
      - if:
          condition:
            voice_assistant.is_running:
          then:
            - voice_assistant.stop:
          else:
            - voice_assistant.start:

# Дополнительно: Индикатор состояния (светодиод на плате)
light:
  - platform: status_led
    id: status_led_light
    name: "Статус LED"
    pin:
      number: GPIO8 # Светодиод на C3 SuperMini
      inverted: true

text_sensor:
  - platform: template
    id: serial_num_sensor
    name: "Serial Number"
    entity_category: diagnostic
    lambda: 'return {"{{ serial_number | default("UNKNOWN") }}"};'
    update_interval: never

  - platform: template
    id: current_ver_sensor
    name: "Current Firmware Version"
    entity_category: diagnostic
    lambda: 'return {"{{ firmware_version }}"};'
    update_interval: never

  - platform: template
    id: available_version_sensor
    name: "Available Firmware Version"
    entity_category: diagnostic
    lambda: 'return id(available_version);'

  - platform: template
    id: changelog_sensor
    name: "Firmware Changelog"
    entity_category: diagnostic

# Настройка прокси для прошивки (помогает при прямом скачивании в HA)
# Включается только если ESPHome собран с поддержкой ota: platform: http_request

script:
  - id: check_firmware_update
    then:
      - http_request.get:
          url: "https://raw.githubusercontent.com/mihazzzold/espHome_SmartStation/main/latest.json"
          request_headers:
            Authorization: !lambda |-
              #ifdef SECRET_GITHUB_TOKEN
                if (std::string(SECRET_GITHUB_TOKEN).length() > 0) {
                  return "token " + std::string(SECRET_GITHUB_TOKEN);
                }
              #endif
              #ifdef SECRET_GITHUB_PAT
                if (std::string(SECRET_GITHUB_PAT).length() > 0) {
                  return "token " + std::string(SECRET_GITHUB_PAT);
                }
              #endif
              return "";
          on_response:
            - lambda: |-
                ESP_LOGI("ota", "Response received from update server");

  - id: install_firmware_update
    then:
      - if:
          condition:
            lambda: 'return id(update_url) != "";'
          then:
            - ota.http_request.flash:
                url: !lambda 'return id(update_url);'
                md5: "00000000000000000000000000000000"

interval:
  - interval: 6h
    then:
      - script.execute: check_firmware_update

button:
  - platform: template
    id: check_update_button
    name: "Check for updates 🔍"
    icon: "mdi:update"
    on_press:
      - script.execute: check_firmware_update

  - platform: template
    id: install_update_button
    name: "Install update ⚡"
    icon: "mdi:download"
    entity_category: config
    on_press:
      - script.execute: install_firmware_update

  - platform: template
    name: "Mic Test (Log) 🎤"
    id: mic_test_button
    icon: "mdi:microphone-settings"
    on_press:
      - logger.log: "Тестирование микрофона (5 сек): проверьте уровень сигнала в логах ESPHome (DEBUG)"
      - voice_assistant.start:
      - delay: 5s
      - voice_assistant.stop:
